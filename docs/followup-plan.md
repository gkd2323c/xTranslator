# 后续工作计划

基于两轮代码审查的遗留项，按影响范围和实施成本排序。

> **执行状态：** 最后一次更新 2026-05-24

---

## 已完成

| 项目 | 说明 |
|------|------|
| A1 `_pics/` 清理 | 旧 UI 设计稿目录已删除 |
| A3 `memory-bank/` 空壳删除 | `project.yaml` 已移除 |
| B1 结构化日志 | `eprintln!` 全部替换为 `log::warn!`，`env_logger` 初始化 |
| C2 拖放 disposed flag | 注释改进，外层 `try-catch` 保留以处理 `getCurrentWebview` 同步异常 |

---

## 未完成项

### B2. `Bin/libhunspell.dll` 构建脚本化（🟢 P2）

**当前状态：** `Bin/x64/libhunspell.dll`（474 KB，2018 年）被 git 跟踪。应用通过 `libloading` 在运行时动态加载。

**分析：**
- 该 DLL 运行时可替换，用户可指定任意 Hunspell DLL 路径
- 自 2018 年加入仓库以来没有变更
- 不是构建的关键依赖（拼写检查功能可无 DLL 运行）

**建议：** 不作处理。DLL 小而稳定，构建脚本化的收益低于成本。

---

### C1. 中英注释统一（🟢 P3）

**当前状态：** 多数模块架构注释使用中文，内联注释混用中英。

**策略：** 不批量化。触及代码时顺手翻译——修改某个模块时同步英文化该模块的注释。

---

## 其余审查项说明

以下审查发现已在报告中记录，属于无需改动或已决策保留的项目：

| 项目 | 决策 |
|------|------|
| agent 配置目录（14+ `.xxx/`） | 保留现状。各工具自动生成，移动后会重新创建 |
| `eprintln!` 对前端不可见 | 已部分解决。`log::warn!` + `env_logger` 输出到 stderr；完整前端事件桥接需要 Tauri 架构改造 |
| 中英注释统一 | 渐进式，随重构推进 |
| 拖放 disposed flag 冗余 | 已审查：外层 `try-catch` 处理 `getCurrentWebview` 同步异常，`.catch()` 处理异步 rejection，两者非冗余 |
